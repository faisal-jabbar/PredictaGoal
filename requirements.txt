pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.1.0
joblib>=1.2.0
python-dotenv>=0.21.0
firebase-admin>=6.0.0
kagglehub>=0.2.0
xgboost>=1.7.0

# Phase 02 — Intelligence Layer
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Phase 02 — Explanations (optional — project works without shap)
shap>=0.44.0

# Phase 02 — Utilities
scipy>=1.10.0
matplotlib>=3.7.0

# Phase 03 — MLflow tracking
mlflow>=2.10.0

# Phase 03 — Prometheus metrics
prometheus-client>=0.19.0

# Phase 03 — Airflow (install separately — see docs/RUNBOOK.md)
# apache-airflow>=2.8.0

# Phase 03 — Email alerts (optional)
# smtplib is part of stdlib — no extra package needed

# Phase 03 — HTTP client for provider checks
httpx>=0.27.0
requests>=2.31.0
